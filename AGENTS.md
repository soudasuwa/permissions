# AI Instructions

- Do not format code.
- Focus only on logic and correctness.
- Assume formatting is handled by `bun run format` before commit.

## Development Notes

Run the following commands before committing:

```
bun run lint
bun run format
bun run build
bun test
```
